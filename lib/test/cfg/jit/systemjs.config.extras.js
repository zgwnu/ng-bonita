/**
 * Add barrels and stuff
 * Adjust as necessary for your application needs.
 */
(function (global) {
    System.config({
        packages: {
        // add packages here
        'dist': { main: 'bundles/ng-bonita.umd.js' }
    }
    });
})(this);