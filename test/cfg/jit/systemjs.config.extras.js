/**
 * Add barrels and stuff
 * Adjust as necessary for your application needs.
 */
(function (global) {
    System.config({
        packages: {
        // add packages here
        'ng-bonita/src': { main: './index.js' },
    }
    });
})(this);
