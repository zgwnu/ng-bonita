export interface ZgwnuBonitaBusinessDataInterface {
    // every Bonita Business Data Object has persistence attributes
    persistenceId: number
    persistenceId_string: string
    persistenceVersion: number
    persistenceVersion_string: string
    // other fields can be defined in child instances  
}
